# =============================================================================
# Projeto Financeiro - Variáveis de ambiente
# Copie para .env na raiz: cp .env.example .env
# O script npm run db:migrate copia o .env da raiz para backend/.env quando necessário.
# =============================================================================

# -----------------------------------------------------------------------------
# Banco de dados (obrigatório para o backend)
# Docker Compose: postgres na porta 5434 do host → use localhost:5434
# -----------------------------------------------------------------------------
DATABASE_URL="postgresql://postgres:postgres@localhost:5434/projeto_financeiro?schema=public"

# -----------------------------------------------------------------------------
# Frontend - URL da API (usada no browser)
# Desenvolvimento local (backend na 3001): http://localhost:3001/api/v1
# Docker (backend na 3011): http://localhost:3011/api/v1
# -----------------------------------------------------------------------------
NEXT_PUBLIC_API_URL=http://localhost:3011/api/v1

# -----------------------------------------------------------------------------
# JWT - Autenticação
# Em produção: use um segredo forte e nunca o commite.
# -----------------------------------------------------------------------------
JWT_SECRET=change-me-in-production
JWT_EXPIRES_IN=7d

# -----------------------------------------------------------------------------
# Telegram (opcional - deixe vazio para desativar)
# Bot: crie em https://t.me/BotFather e cole o token apenas no .env local.
# Webhook: URL pública (em dev use ngrok). Depois: cd backend && npm run telegram:webhook
# SEGURANÇA: Nunca commite .env com token preenchido.
# -----------------------------------------------------------------------------
TELEGRAM_BOT_TOKEN=
TELEGRAM_WEBHOOK_URL=

# -----------------------------------------------------------------------------
# Jobs agendados (cron) no backend
# 1 = desativa o job (útil em testes ou se rodar jobs externamente)
# -----------------------------------------------------------------------------
CRON_RECURRING_DISABLED=0
CRON_ALERTS_TELEGRAM_DISABLED=0
