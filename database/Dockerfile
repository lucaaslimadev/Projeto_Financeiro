# PostgreSQL para o Projeto Financeiro
# Uso: docker build -t projeto-financeiro-db ./database
# Ou via docker-compose (imagem postgres:16-alpine é usada diretamente no compose)

FROM postgres:16-alpine

ENV POSTGRES_USER=postgres
ENV POSTGRES_PASSWORD=postgres
ENV POSTGRES_DB=projeto_financeiro

EXPOSE 5432
